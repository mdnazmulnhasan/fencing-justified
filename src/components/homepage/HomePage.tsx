import React from 'react';
import ProductSlider from '../products/ProductSlider';

const HomePage = () => {
    return (
        <div>
            <ProductSlider />
        </div>
    );
};

export default HomePage;